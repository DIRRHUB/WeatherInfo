import 'package:flutter/material.dart';

class CityMenuWidget extends StatelessWidget {
  const CityMenuWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
